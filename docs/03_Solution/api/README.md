# docs/03_Solution/api/

Reserved for API design documentation (FastAPI service contracts/endpoints per the technology
stack in `docs/00_Project_Governance/STD/01_project_standards.md`). No content has been
written yet — this is distinct from the actual implemented FastAPI code, which now lives under
`api/` (Tier 0 Bootstrap RBAC endpoints; see `api/README.md`), not `backend/` (the removed
Django prototype). See `docs/PROJECT_DOCUMENTATION.md` → Conventions & gotchas.
