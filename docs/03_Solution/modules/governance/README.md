# NSS ERP Governance Module

Status: NOT STARTED (Solution design) — `backend/governance/` exists as a Django app stub
(empty `models.py`/`views.py`, no `urls.py`, not in `INSTALLED_APPS`).

Reserved for the Governance module's Solution-level design documents (General Body, Governing
Body, Advisory Board, Committees, Position Assignment), following the same `01_design` /
`02_erd` / `03_business_rules` / `04_table_design` pattern used by
`docs/03_Solution/modules/person/` (organization has since moved to a different 5-file
pattern — see `docs/03_Solution/modules/organization/README.md`). Not to be confused with
`docs/00_Project_Governance/`, which governs the *project itself*, not this ERP module. No
content has been written yet.
