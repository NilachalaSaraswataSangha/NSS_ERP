# docs/04_Testing/api/

Reserved for API test documentation/plans. No content has been written yet. The FastAPI Tier 0
API layer (`api/`) is now implemented (read-only bootstrap endpoints, no automated tests yet —
see root `CLAUDE.md` → Running the FastAPI API and → Tests & lint).
