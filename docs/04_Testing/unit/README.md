# docs/04_Testing/unit/

Reserved for unit test documentation/plans. No content has been written yet — no test framework
is configured for the FastAPI `api/` layer yet, see root `CLAUDE.md` → Tests & lint. The earlier
Django prototype (`backend/`) that this note used to reference was removed in the
`feature/fastapi-tier0` branch; it is preserved in Git history only.
